% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/st_union.R
\name{st_union_no_overlaps}
\alias{st_union_no_overlaps}
\title{Unión espacial completa sin superposiciones (Planarize)}
\usage{
st_union_no_overlaps(x, y, sel_var_y = dplyr::everything())
}
\arguments{
\item{x}{Objeto \code{sf} de polígonos.}

\item{y}{Objeto \code{sf} de polígonos.}

\item{sel_var_y}{Selección de variables de \code{y} a mantener (tidy-select).}
}
\value{
Un objeto \code{sf} de polígonos.
}
\description{
Crea una cobertura completa de las geometrías de \code{x} e \code{y}, resolviendo superposiciones.
El resultado incluye:
\enumerate{
\item La intersección de \code{x} e \code{y}.
\item La diferencia de \code{y} menos \code{x}.
\item La diferencia de \code{x} menos \code{y}.
}

Es útil para generar un mosaico de polígonos sin traslapes a partir de dos capas.
}
\examples{
\dontrun{
  # mosaico <- st_union_no_overlaps(zona_a, zona_b)
}
}
