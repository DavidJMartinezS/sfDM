% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/st_ziprar.R
\name{read_sf_ziprar}
\alias{read_sf_ziprar}
\alias{copy_sf_ziprar}
\title{Leer o copiar shapefiles desde un archivo comprimido (.zip o .rar)}
\usage{
read_sf_ziprar(zip.file, pattern)

copy_sf_ziprar(zip.file, pattern, dirsave)
}
\arguments{
\item{zip.file}{Ruta al archivo comprimido (\code{.zip} o \code{.rar}). Debe ser una única ruta.}

\item{pattern}{Patrón de expresión regular (regex) para buscar el nombre del shapefile
dentro del archivo comprimido. La búsqueda no distingue mayúsculas de minúsculas.}

\item{dirsave}{Directorio de destino donde se copiarán los archivos (solo para \code{copy_sf_ziprar}).}
}
\value{
\code{read_sf_ziprar}: Si se encuentra un solo shapefile, devuelve un objeto \code{sf}.
Si se encuentran múltiples shapefiles, devuelve una lista nombrada de objetos \code{sf}.
Si no se encuentra ningún shapefile que coincida, devuelve \code{NULL} con una advertencia.

\code{copy_sf_ziprar}: Retorna \code{NULL} de forma invisible. Emite una advertencia si no se encuentra
el shapefile.
}
\description{
\code{read_sf_ziprar} lee uno o más shapefiles desde un archivo \code{.zip} o \code{.rar}
que coincidan con un patrón de búsqueda, devolviendo un objeto \code{sf} o una lista de ellos.

\code{copy_sf_ziprar} copia los archivos componentes de un shapefile (p. ej. .shp, .shx, .dbf)
desde un archivo \code{.zip} o \code{.rar} a un directorio específico.

Ambas funciones requieren que 7-Zip esté instalado y accesible en el PATH del
sistema para poder manejar archivos \code{.rar}.
}
\examples{
# Se necesita un archivo .zip de ejemplo para ejecutar.
# zip_path <- "ruta/a/su/archivo.zip"
# if (file.exists(zip_path)) {
#   # Leer un shapefile cuyo nombre contenga 'comunas'
#   comunas_sf <- read_sf_ziprar(zip_path, pattern = "comunas")
# }
#
# dest_dir <- tempdir()
# if (file.exists(zip_path) && dir.exists(dest_dir)) {
#   # Copiar los archivos del shapefile 'comunas' a un directorio
#   copy_sf_ziprar(zip_path, pattern = "comunas", dirsave = dest_dir)
# }
}
