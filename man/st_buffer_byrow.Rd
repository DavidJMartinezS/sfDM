% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/st_buffer_byrow.R
\name{st_buffer_byrow}
\alias{st_buffer_byrow}
\title{Buffer variable por fila}
\usage{
st_buffer_byrow(sf, field)
}
\arguments{
\item{sf}{Objeto de clase \code{sf}.}

\item{field}{Nombre o posición del campo que contiene la distancia del buffer en metros para cada fila.}
}
\value{
Un objeto \code{sf} con las geometrías resultantes del buffer. Las filas con valores \code{NA}
en la columna de distancia son eliminadas.
}
\description{
Aplica un buffer a cada geometría del objeto \code{sf} utilizando una distancia variable
especificada en una columna del mismo objeto.
}
\examples{
\dontrun{
  # Usando nombre de columna como texto
  sf_buffer <- st_buffer_byrow(sf_obj, "distancia_m")
  
  # Usando nombre de columna sin comillas
  sf_buffer <- st_buffer_byrow(sf_obj, distancia_m)
  
  # Usando posición de la columna
  sf_buffer <- st_buffer_byrow(sf_obj, 1)
}
}
